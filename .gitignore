node_modules
dist
.env
.env.local
.claude

.agents
.vercel

.env*
300_savol_javob_rasmlar/*
!300_savol_javob_rasmlar/300_savol_javob.json
# Bundled serverless artifacts — Vercel rebuilds each deploy via build:api (audit hygiene: stale-bundle git bloat)
# NOTE: api/index.js va api/bot.js stub'lari TRACKED bo'lishi SHART — Vercel CLI 59+
# vercel.json dagi `functions` pattern'larni buildCommand'dan OLDIN tekshiradi.
api/dist/

# video analysis temp
video.mp4
frames/
.webhook-secret.tmp

# debug logs
vercel-dev.log
vlogs.log

# terminal crash dumps
mintty.*.html

# Graphify local index artifact (rebuilt via: graphify update .)
graphify-out/

.opencode/

# Test ad-hoc skrinshotlar (Playwright/MCP sessiyalar — rootga tushadi)
.playwright-mcp/
playwright-report/
test-results/
coverage/
*.png
.idea/
.jdks/

# Mahalliy sessiya qaydlari (davomiylik hujjati — commit qilinmaydi)
walkthrough.md
